[package]
name = "myscelium_engine"
version = "1.3.0"
edition = "2021"
description = "Myscelium is a library designed to simplify the creation of large, interconnected networks of Python scripts through sockets. These scripts harmoniously run on different machines, yet synchronize with one another. Much like the hyphae in mycelium interconnect various mushrooms, the socket bridges in the Myscelium library interlink multiple machines, synchronizing commands and harnessing the power of modularity."
authors = ["Cristian Camargo Filho <ccf@cdone.com.br>"]

[profile.release]
opt-level = 3

[lib]
path = "src/lib.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde_json = "1.0.128"
serde = { version = "1.0.210", features = ["derive"] }
indexmap = { version = "2.5.0", features = ["serde"] }
rusqlite = { version = "*", features = ["bundled"] }
lazy_static = "1.5.0"
rand = "0.8.5"
chrono = "0.4.38"
ctrlc = "3.4.5"
cpython = "0.7.2"
parking_lot = { version = "0.12.3", features = ["deadlock_detection"] }
OxidizedMyscelium = { path = "../OxidizedMysceliumCore/OxidizedMyscelium" }
oxidized_myscelium_macros = { path = "../OxidizedMysceliumCore/oxidized_myscelium_macros" }
RustPyNet = { path = "../RustPyNet/RustPyNet" }

[dependencies.pyo3]
version = "0.19.2"
features = ["extension-module"]
